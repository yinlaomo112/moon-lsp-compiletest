name = "yinlaomo112/moon-lsp-compiletest"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/yinlaomo112/moon-lsp-compiletest"

license = "Apache-2.0"

keywords = [ "lsp", "diagnostics", "testing", "compiletest", "moonbit" ]

description = "MoonBit-native compiletest-style regression runner for moon-lsp publishDiagnostics across document changes."

preferred_target = "native"

source = "src"

import {
  "moonbitlang/async@0.20.3",
}
